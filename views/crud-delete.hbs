<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Deteletar Comida {{comida.name}}</title>
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
</head>

<body>
    <div class="mt-8 container mx-auto">
        <h1 class="text-4xl mb-8">Deletar {{comida.name}} {{comida.emoji}}??</h1>
        <form class="w-full max-w-sm" method="POST">
            <input type="hidden" name="id" value="{{comida.id}}">
            <div class="md:flex md:items-center">
                <div class="md:w-1/3">
                    <button
                        class="shadow bg-gray-500 hover:bg-gray-400 focus:shadow-outline focus:outline-none text-white font-bold py-2 px-4 rounded"
                        type="button" onclick="javascript: window.history.back()">
                        Cancelar
                    </button>
                </div>
                <div class="md:w-2/3">
                    <button
                        class="shadow bg-red-500 hover:bg-red-400 focus:shadow-outline focus:outline-none text-white font-bold py-2 px-4 rounded"
                        type="submit">
                        Deletar {{comida.name}}!
                    </button>
                </div>
            </div>
        </form>
    </div>


</body>

</html>